#include <iostream>
#include <string>
using namespace std;

void getPlayerNames(string &playerX, string &playerO) {
    cout << "Enter name for Player X: ";
    cin >> playerX;
    cout << "Enter name for Player O: ";
    cin >> playerO;
}

